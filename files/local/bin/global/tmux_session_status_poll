#!/usr/bin/env bash
# Wrapper called via #() in status-format to keep periodic polling alive.
# Runs tmux_session_status (which updates @session_bar), then outputs nothing
# — display comes from #{@session_bar}.

tmux_session_status &>/dev/null
